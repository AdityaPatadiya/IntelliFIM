# =============================================================================
# IntelliFIM - Development requirements
# Install via:  pip install -r requirements/dev.txt
# =============================================================================

-r base.txt

# ---------- Test framework ----------
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
pytest-cov>=5.0,<7.0
pytest-asyncio>=0.23,<1.0
pytest-xdist>=3.5,<4.0           # parallel test execution
factory-boy>=3.3,<4.0
faker>=25.0

# ---------- Dev tooling ----------
django-debug-toolbar>=4.3,<6.0
django-extensions>=3.2,<5.0      # gives shell_plus, runserver_plus, etc.
ipython>=8.0                     # nicer Django shell

# ---------- Linting / formatting / typing ----------
ruff>=0.5                        # fast linter + formatter (replaces flake8 + black)
mypy>=1.10
django-stubs[compatible-mypy]>=5.0
djangorestframework-stubs[compatible-mypy]>=3.15

# ---------- Security scanning (optional but recommended) ----------
bandit>=1.7
pip-audit>=2.7

# ---------- Pinning helper (optional) ----------
# Run `pip-compile requirements/base.in` etc. once we move to .in files for
# fully reproducible builds. Not required day one.
pip-tools>=7.4
